version: "3.8"
services:
  db:
    image: postgres
    restart: unless-stopped
    volumes:
      - ./volumes/db:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tcg_explorer
  web:
    image: kelu/tcgexplorer:${VERSION}
    restart: unless-stopped
    environment:
      SECRET_KEY_BASE: ${SECRET_KEY_BASE}
      DATABASE_URL: ${DATABASE_URL}
      HOST: ${HOST}
      VERSION: ${VERSION}
    ports:
      - "${PORT}:4000"
    depends_on:
      - db
